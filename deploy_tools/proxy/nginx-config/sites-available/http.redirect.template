# Automatically redirect all http traffic to https
server {
	listen 80 default_server;
	listen [::]:80 default_server;

	server_name _;

	return ${REDIRECT_CODE} https://${DOLLAR}host${DOLLAR}request_uri;
}
